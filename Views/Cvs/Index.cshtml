@model IReadOnlyList<CvManagementSystem.ViewModels.CvListViewModel>

@{
    ViewData["Title"] = L["CVs"].Value;

    var positions =
        ViewBag.Positions as IEnumerable<dynamic>
        ?? Enumerable.Empty<dynamic>();

    var search = ViewBag.Search as string;
    int? selectedPosition = ViewBag.PositionId as int?;
    bool isRecruiter = ViewBag.IsRecruiter ?? false;
    bool isAdministrator = ViewBag.IsAdministrator ?? false;
}

<div class="container py-4">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h3 mb-1">@L["CVs"]</h1>
            <p class="text-muted mb-0">
                @(isRecruiter || isAdministrator
                    ? L["BrowseCandidateCvs"]
                    : L["ManageYourCvs"])
            </p>
        </div>

        <a asp-controller="Positions"
           asp-action="Index"
           class="btn btn-outline-primary">
            @L["BrowsePositions"]
        </a>
    </div>

    @if (TempData["SuccessMessage"] is string success)
    {
        <div class="alert alert-success">
            @success
        </div>
    }

    @if (TempData["ErrorMessage"] is string error)
    {
        <div class="alert alert-danger">
            @error
        </div>
    }

    <div class="card shadow-sm mb-4">
        <div class="card-body">

            <form asp-action="Index"
                  method="get"
                  class="row g-3">

                <div class="col-md-6">
                    <label for="search"
                           class="form-label">
                        @L["SearchCvs"]
                    </label>

                    <input id="search"
                           name="search"
                           value="@search"
                           class="form-control"
                           placeholder="@L["CvSearchPlaceholder"]" />
                           <input type="hidden"
                               name="tag"
                               value="@ViewBag.Tag" />
                </div>

                <div class="col-md-4">
                    <label for="positionId"
                           class="form-label">
                        @L["Position"]
                    </label>

                    <select id="positionId"
                            name="positionId"
                            class="form-select">

                        <option value="">
                            @L["AllPositions"]
                        </option>

                        @foreach (var position in positions)
                        {
                            <option value="@position.Id"
                                    selected="@(selectedPosition == (int)position.Id)">
                                @position.Title
                            </option>
                        }

                    </select>
                </div>

                <div class="col-md-2 d-flex align-items-end">
                    <div class="d-flex gap-2 w-100">

                        <button type="submit"
                                class="btn btn-primary flex-grow-1">
                            @L["Search"]
                        </button>

                        <a asp-action="Index"
                           class="btn btn-outline-secondary">
                            @L["Clear"]
                        </a>

                    </div>
                </div>

            </form>

        </div>
    </div>

    @if (!string.IsNullOrWhiteSpace(ViewBag.Tag as string))
    {
        <div class="alert alert-info">
            @L["ShowingResultsForTag"] <strong>@ViewBag.Tag</strong>
        </div>
    }

    <div class="card shadow-sm">

        <div class="card-body p-0">

            @if (Model.Count == 0)
            {
                <div class="p-4 text-center">
                    <h5 class="mb-2">@L["NoCvsFound"]</h5>
                    <p class="text-muted mb-3">
                        @L["NoCvsMatchingFilters"]
                    </p>

                    <a asp-controller="Positions"
                       asp-action="Index"
                       class="btn btn-primary">
                        @L["ViewPositions"]
                    </a>
                </div>
            }
            else
            {
                <div class="table-responsive">

                    <table class="table table-hover align-middle mb-0">

                        <thead class="table-light">
                            <tr>
                                <th>@L["CV"]</th>
                                <th>@L["Position"]</th>
                                @if (isRecruiter || isAdministrator)
                                {
                                    <th>@L["Candidate"]</th>
                                }

                                <th>@L["Status"]</th>
                                <th>@L["Likes"]</th>
                                <th>@L["Updated"]</th>
                            </tr>
                        </thead>

                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr style="cursor:pointer"
                                    onclick="window.location='@Url.Action("Details", "Cvs", new { id = item.Id })'">

                                    <td>
                                        <a asp-action="Details"
                                           asp-route-id="@item.Id"
                                           class="fw-semibold text-decoration-none"
                                           onclick="event.stopPropagation();">
                                            @item.Title
                                        </a>
                                    </td>

                                    <td>
                                        @item.PositionTitle
                                    </td>

                                    @if (isRecruiter || isAdministrator)
                                    {
                                        <td>
                                            @item.CandidateName
                                        </td>
                                    }

                                    <td>
                                        @if (item.IsPublished)
                                        {
                                            <span class="badge text-bg-success">
                                                @L["Published"]
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="badge text-bg-secondary">
                                                @L["Draft"]
                                            </span>
                                        }
                                    </td>

                                    <td>
                                        @item.LikeCount
                                    </td>

                                    <td>
                                        @(item.UpdatedAt.ToLocalTime()
                                            .ToString("dd MMM yyyy, HH:mm"))
                                    </td>
                                </tr>
                            }
                        </tbody>

                    </table>

                </div>
            }

        </div>

    </div>

</div>
