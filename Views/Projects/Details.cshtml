@model CvManagementSystem.ViewModels.ProjectViewModel

@{
    ViewData["Title"] = "Project Details";

    bool isAdministratorView =
        ViewBag.IsAdministratorView is bool value && value;
}

<div class="container py-4">

    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-4">

        <div>
            <h1 class="h3 mb-1">
                @(Model.Name)
            </h1>

            <p class="text-muted mb-0">
                @(Model.Period)
            </p>
        </div>

        <a asp-action="Index"
           asp-route-userId="@(isAdministratorView ? Model.UserId : null)"
           class="btn btn-outline-secondary">
            Back to Projects
        </a>

    </div>

    <div class="card shadow-sm mb-4">

        <div class="card-header">
            <strong>Project Information</strong>
        </div>

        <div class="card-body">

            <dl class="row mb-0">

                <dt class="col-sm-3">
                    Project Name
                </dt>

                <dd class="col-sm-9">
                    @(Model.Name)
                </dd>

                <dt class="col-sm-3">
                    Period
                </dt>

                <dd class="col-sm-9">
                    @(Model.Period)
                </dd>

                <dt class="col-sm-3">
                    Start Date
                </dt>

                <dd class="col-sm-9">
                    @(Model.StartDate.ToString("dd MMM yyyy"))
                </dd>

                <dt class="col-sm-3">
                    End Date
                </dt>

                <dd class="col-sm-9">

                    @if (Model.EndDate.HasValue)
                    {
                        @(Model.EndDate.Value.ToString("dd MMM yyyy"))
                    }
                    else
                    {
                        <span class="text-muted">
                            Present
                        </span>
                    }

                </dd>

            </dl>

        </div>

    </div>

    <div class="card shadow-sm mb-4">

        <div class="card-header">
            <strong>Description</strong>
        </div>

        <div class="card-body">

            <div class="border rounded p-3">
                @(Model.DescriptionMarkdown)
            </div>

            <div class="form-text mt-2">
                This content is stored as Markdown. Markdown rendering can
                be added when we build the final CV renderer.
            </div>

        </div>

    </div>

    <div class="card shadow-sm mb-4">

        <div class="card-header">
            <strong>Technology Tags</strong>
        </div>

        <div class="card-body">

            @if (Model.TechnologyTags.Count == 0)
            {
                <span class="text-muted">
                    No technology tags have been added.
                </span>
            }
            else
            {
                <div class="d-flex flex-wrap gap-2">

                    @foreach (var tag in Model.TechnologyTags)
                    {
                        <span class="badge text-bg-light p-2">
                            @(tag)
                        </span>
                    }

                </div>
            }

        </div>

    </div>

</div>