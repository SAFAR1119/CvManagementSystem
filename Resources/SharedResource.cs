// The resource files live in Resources/, so the marker type must use the
// same namespace.  Otherwise IStringLocalizer looks for
// CvManagementSystem.SharedResource.* instead of the embedded
// CvManagementSystem.Resources.SharedResource.* resource set.
namespace CvManagementSystem.Resources;

public class SharedResource
{
}
